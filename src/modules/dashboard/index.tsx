'use client';

import { Box } from '@mui/material/';

export const Dashboard = () => {
  return (
    <Box sx={{display: 'flex', flexDirection: 'column'}}>
       <h4>Dashboard</h4>
    </Box>
  );
};
